# Gemini Adapter (Thin Shim)

Canonical workflow, standards, and shared context live in:

- `AGENTS.md`
- `specs/guides/`
- `specs/active/{slug}/` (when working on a feature)

## What to load as context

- Always read `AGENTS.md` first.
- Then read `specs/guides/README.md` and any relevant guides.
- For active work: read the workspace `README.md` (context pack), `prd.md`, `tasks.md`, `recovery.md`.

## Local commands

Prefer the repo’s existing targets and documented commands:

- See `CLAUDE.md` for the authoritative command list for install/test/lint/docs in this repo.

